# Visual workflow — proofs and review surfaces

First-pass sort of the proof and review-surface pattern.

- Scope pages: `proofs.html`, `style.html`, `location.html` on
  situationshipin.space, with the shared `assets/css/review.css` and
  `assets/js/review.js` (local draft storage, download JSON, GitHub issue).
- Per-artifact review pages: `review/…-filmstrip/` and `review/…-proxy/`
  directories with `index.html` + `manifest.json`, media under
  `review/media/…`.
- Review decisions are recorded as GitHub issues
  (`bdekoz/situationshipin.space`) and mirrored into izzi dyads under
  `docs/training-conversion/dyads/`.

Status: `FIRST-PASS-SORT; NO-REMOVAL`
